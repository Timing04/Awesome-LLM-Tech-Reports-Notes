# Models

这个目录存放模型家族下的主笔记。

约定:

- 一篇模型报告只保留一份正文
- 文件名使用稳定 slug，例如 `qwen2.md`、`deepseek_v3.md`
- 相关横向总结放到 `topics/`，不要在多个家族目录重复写正文

## 当前主线家族

- `llama`
- `qwen`
- `deepseek`
- `mistral`
- `gemma`
- `yi` / `internlm`

## 扩展候选家族（已进入元数据）

- `baichuan`
- `chatglm`
- `zhipu`（GLM 系列）
- `moonshot`（Kimi 系列）
- `minimax`
- `stepfun`（阶跃星辰）
- `modelbest`（面壁智能 / MiniCPM 系列）

## 建议阅读顺序

1. 先读 `llama` / `qwen` / `deepseek` / `mistral` 主线，建立通用大图景。
2. 再读中文与多模态主流家族（如 `chatglm`、`moonshot`、`modelbest`）理解应用化路线。
3. 最后回到 `topics/` 做横向比较（MoE、Long Context、Post-training）。
